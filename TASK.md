# TASK.md — План реализации MVP LumenBridge

Этот файл отражает текущий прогресс разработки. Claude Code должен обновлять статусы задач по мере их выполнения, двигаясь строго последовательно.

## Этап 1: Инициализация и окружение
- [x] 1.1. Создать структуру монорепозитория (папки `frontend` и `backend`).
- [x] 1.2. Настроить конфигурационные файлы (`package.json`, TypeScript, ESLint) в обоих проектах.
- [x] 1.3. Настроить Tailwind CSS v4 во `frontend` и перенести туда CSS-переменные из дизайн-токенов AGENTS.md.
- [x] 1.4. Проверить конфигурацию окружения (создать `.env.example` и `.env`).

## Этап 2: База данных и Core-бэкэнд
- [x] 2.1. Инициализировать Prisma/TypeORM в `backend`, описать схему БД из AGENTS.md (User, Application, Loan, PaymentSchedule, PaymentConfirmation).
- [x] 2.2. Создать и применить первую миграцию в PostgreSQL.
- [x] 2.3. Написать скрипт сидирования (`seed.ts`) для создания тестового Admin и Operator.
- [x] 2.4. Реализовать математический модуль расчёта аннуитета на бэкенде (формула из AGENTS.md) и покрыть его базовыми unit-тестами.

## Этап 3: Аутентификация и Роли (API)
- [x] 3.1. Реализовать эндпоинт авторизации/регистрации клиентов по телефону с имитацией SMS OTP (логирование кода в консоль бэкенда).
- [x] 3.2. Реализовать эндпоинт входа для персонала по email/password (JWT).
- [x] 3.3. Настроить RBAC (Guards) для разделения доступа: `client`, `operator`, `admin`.

## Этап 4: Публичный фронтенд и Калькулятор
- [x] 4.1. Развернуть структуру папок Next.js по методологии FSD (Feature-Sliced Design).
- [x] 4.2. Настроить Layouts (хедер, футер) в цветах `--color-midnight` / `--color-white`.
- [x] 4.3. Сверстать Главную страницу и Кредитный калькулятор (мгновенный расчёт на клиенте по формуле, без дебаунса).
- [x] 4.4. Добавить текстовые страницы: "Как это работает", "Бизнес-займы", FAQ и юридические документы.

## Этап 5: Личный кабинет клиента (ЛК)
- [x] 5.1. Создать защищенную зону `/cabinet` с Middleware-проверкой сессии.
- [x] 5.2. Реализовать форму заявки на займ (физ. лицо / бизнес, Valibot, калькулятор передаёт параметры).
- [x] 5.3. Сверстать экран активного займа: плашка ожидания подписи -> кнопка "Подписать" -> ввод mock-OTP -> фиксация `signed_at`, IP и User Agent.
- [x] 5.4. Вывод интерактивного графика платежей и формы отправки уведомления об оплате (ввод суммы и reference).

## Этап 6: Панель Администратора
- [x] 6.1. Реализовать интерфейс входа `/admin/login`.
- [x] 6.2. Создать дашборд оператора: список новых заявок, кнопка смены статуса на `processing`, кнопка "Одобрить" (создание Loan).
- [x] 6.3. Интерфейс подтверждения платежей пользователей: просмотр референсов, кнопка "Подтвердить" с триггером автоматического перерасчёта графика.

## Этап 7: Финализация и Проверка DoD
- [ ] 7.1. Проверить обработку ошибок API (401, 403, 400) на фронтенде и заменить все лоадеры на Skeletons.
- [ ] 7.2. Провести сквозной тест: от ползунка калькулятора до закрытия займа оператором.
- [ ] 7.3. Сгенерировать итоговый README.md с инструкцией по запуску.
